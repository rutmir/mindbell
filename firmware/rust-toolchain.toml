[toolchain]
# Тулчейн ставится через espup (см. README). Для ESP32-C3 (RISC-V) канал "esp"
# работает; при желании можно перейти на стабильный nightly с build-std, но
# "esp" — самый протоптанный путь.
channel = "esp"
