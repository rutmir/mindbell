[toolchain]
# Свежий тулчейн "esp" (Rust ≥1.85): edition2024-зависимости и новые крейты
# собираются без пинов версий. Проблему знаковости c_char в esp-idf-svc 0.49.1
# решаем точечным патчем крейта (см. [patch.crates-io] в Cargo.toml и
# vendor/esp-idf-svc), а не откатом тулчейна.
channel = "esp"
