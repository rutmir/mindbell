# MindBell — заметки по железу и архитектуре

Носимое устройство-«напоминалка»: ESP32-C3 даёт короткие виброимпульсы по
настраиваемому расписанию (диапазоны времени + интервалы), настройка по BLE с
телефона. Прошивка — Rust, приложение — Flutter.

---

## 1. Концепция и архитектура

- **Носимое, компактное, постоянно с собой.** Питание — встроенный LiPo
  602040 450 мА·ч (3.7V), заряд по USB-C.
- **Постоянная связь не нужна.** Телефон подключается по запросу: читает
  параметры и уровень заряда, передаёт новые настройки.
- **Главный архитектурный принцип: устройство живёт в deep sleep, а не висит в
  BLE.** Внутренний RTC-таймер ESP32-C3 продолжает считать время в deep sleep и
  будит по расписанию → импульс на мотор → снова сон. Иначе 450 мА·ч хватит на
  12–30 часов.
- **Следствие — нужна кнопка для режима настройки.** В deep sleep радио
  выключено, телефон не может подключиться в произвольный момент. Нажатие кнопки
  будит устройство, поднимает BLE-рекламу на 30–60 с для синхронизации, затем
  снова сон. Кнопка заодно = вкл/выкл.

### Учёт времени
- Внутренний RTC ESP32-C3 считает время, пока есть питание (в т.ч. в deep
  sleep). Внешний RTC (DS3231) не нужен, т.к. устройство постоянно на
  аккумуляторе.
- Телефон при подключении синхронизирует время (отправляет Unix timestamp).
- Заложить абстракцию `TimeSource` на случай замены.

### Модель конфигурации
- Расписание = массив сегментов `{ start: HH:MM, end: HH:MM, interval_min, enabled }`.
- Хранить в NVS (флеш), чтобы переживало перезагрузку/сон.
- Передавать с телефона одним blob (CBOR/JSON) через одну BLE-характеристику.
- На прошивке обработать пересечения и «дырки» в сегментах.

### Софт-стек
- Прошивка: **Rust на `esp-idf-svc`/`esp-idf-hal`** (std, поверх ESP-IDF) —
  стабильный BLE (NimBLE) из коробки. `esp-hal` (no_std) пока сыроват по BLE.
- Приложение: **Flutter + `flutter_blue_plus`**. Учесть рантайм-разрешения
  Bluetooth/Location на Android и entitlements на iOS — BLE-UX обычно дороже,
  чем сама прошивка.

---

## 2. Питание — КЛЮЧЕВОЙ блок

**Главное число для носимого = ток покоя всей силовой цепи в deep sleep.**
Экономия от deep sleep ESP32 (микроамперы) бессмысленна, если силовая обвязка
сама тянет миллиамперы. Это число надо ИЗМЕРИТЬ мультиметром до закрытия корпуса.

### Имеющиеся у пользователя модули и их роли (важно не путать)
- **TP4056** — заряжает LiPo от 5V с USB. Выход → на батарею. Единственная роль.
- **MT3608** — повышающий (boost) преобразователь. К ЗАРЯДУ отношения НЕ имеет,
  это часть цепи питания/разряда.
- **YP-8** — линейный стабилизатор 3.3V (LDO), питает плату.

Предполагаемая схема пользователя: `Батарея → MT3608 (boost) → YP-8 (LDO) → 3.3V`.
Электрически работает, НО для носимого на deep sleep плоха из-за тока покоя:
- MT3608 в покое тянет ~2–6 мА постоянно, выключить нельзя.
- YP-8 (если AMS1117) — ещё ~5–10 мА тока покоя.
- Итог: ~8 мА покоя → 450/8 ≈ **56 часов просто лёжа в кармане**. Deep sleep
  обесценен.

### РЕКОМЕНДУЕМАЯ схема питания
1. **Убрать MT3608.** ESP32-C3 работает до ~3.0V (порог brownout настраивается).
   LiPo отдаёт почти весь заряд в диапазоне 3.6–4.2V (выше 3.3V), boost не нужен.
2. **Заменить YP-8/AMS1117 на low-Iq LDO:** **HT7333** (Iq ~4 мкА, dropout ~0.1V)
   или MCP1700. Схема: `Батарея → HT7333 → 3.3V → ESP32`.
3. **TP4056 — брать версию с защитой** (TP4056 + DW01/8205, два чипа) для защиты
   LiPo от переразряда.
4. **Зарядка одновременно с работой (load sharing):** голый TP4056 неправильно
   определяет конец заряда под нагрузкой. Для устройства, которое заряжают
   снятым/выключенным — терпимо. Иначе нужна схема load-sharing.

С low-Iq LDO без boost ток покоя падает до единиц мкА, и автономность упирается
в саморазряд LiPo, а не в схему.

### Измерение уровня заряда
- Через ADC по делителю напряжения от батареи (4.2V ужать до <3.3V для ADC).
- Напряжение LiPo → процент нелинейно: отдавать в приложение голое напряжение
  или 3–4 градации, а не точные проценты.

---

## 3. Вибромотор 1030 3V + MOSFET

### Мотор
- 1030 (10×3 мм), рабочий ток ~80–120 мА, пусковой/stall до ~150–200 мА.
- По току для коммутации это очень мало — любой N-MOSFET справится с запасом.
  Вопрос НЕ в токе, а в пороге открытия.

### MOSFET (снят с BMS 18650 5S2P, маркировка ~«BP3 100K»)
- Маркировку надёжно опознать НЕ удалось (похоже на лот-код).
- С BMS почти наверняка N-канальный → годится для low-side ключа (мотор между
  «+» и стоком, исток на землю, гейт от GPIO).
- **РИСК: может быть не logic-level.** Силовые ключи BMS бывают рассчитаны на
  Vgs 4.5–10V. От 3.3V откроется частично → греется, падает напряжение, мотор
  вибрирует слабо.

### Тест logic-level (мультиметром, ~2 мин)
1. Собрать low-side ключ, подать 3.3V на гейт.
2. Замерить Vds (сток-исток) при работающем моторе:
   - **< 50–100 мВ, транзистор холодный** → отлично, открывается полностью.
   - **≥ 0.2V + нагрев** → НЕ logic-level, заменить.
3. Убедиться, что мотор крутится на полную.

Если тест провален — взять **AO3400** (SOT-23, открывается от 2.5V, до ~5A,
компактнее BMS-ключа) или 2N7002 (на пределе по току для 100 мА).

### Обязательная обвязка ключа
1. **Pulldown гейт→исток (10–100 кОм)** — КРИТИЧНО для deep sleep. В Hi-Z (сон,
   загрузка) гейт наводится → мотор самопроизвольно жужжит. Резистор держит
   транзистор закрытым в покое.
2. **Flyback-диод параллельно мотору** (катод к «+»), 1N4148/SS14 — гасит
   индуктивный выброс.
3. **Резистор в гейт последовательно (100–470 Ом)** — ограничивает бросок и звон
   (не обязателен на 100 мА).
4. **Конденсатор 100–470 мкФ у батареи** — пусковой ток мотора не должен ронять
   питание ESP32 в brownout.

Примечание: на разряженной батарее (3.4V) вибрация слабее, чем на полной (4.1V).
Для «напоминалки» вероятно некритично.

---

## 4. Финальные схемы

### 4.1 Питание и заряд

```
   USB-C 5V (заряд)
       │
   ┌───┴─────────────────┐
   │  TP4056 + DW01/8205  │   ← версия С ЗАЩИТОЙ (2 чипа)
   │  (защита от пере-    │
   │   заряда/разряда)    │
   │                      │
   │  B+            B-     │
   └───┬────────────┬─────┘
       │            │
     LiPo+        LiPo-
   602040 450мА·ч 3.7V
       │            │
       │            └──────────────────────────┬──── GND (общая земля)
       │                                        │
  VBAT (3.0–4.2V) ─────┬──────────────┐         │
       │               │              │         │
       │           [C_bulk]      → к мотору     │
   ┌───┴─────┐     100–470мкФ     (раздел 4.2)  │
   │ HT7333  │         │                        │
   │ VIN GND ├─────────┴────────────────────────┤
   │  VOUT   │                                  │
   └───┬─────┘                                  │
       │ 3.3V                                   │
       ├──────────┬────────────┐                │
       │      [C 100нФ]     [C 10мкФ]            │
    ESP32-C3 3V3      │            │             │
    ESP32-C3 GND ─────┴────────────┴─────────────┘
```

Ключевое:
- **MT3608 исключён** — ESP32-C3 работает до ~3.0V, boost не нужен.
- **HT7333** (low-Iq LDO, Iq ~4 мкА) вместо YP-8/AMS1117 — ради тока покоя.
- **Мотор питается от VBAT (сырая батарея), а не от 3.3V** — ток в обход LDO.
- `C_bulk` 100–470 мкФ на VBAT у мотора — гасит просадку от пускового тока
  (защита от brownout).
- TP4056 с защитой; load-sharing нет → **заряжать выключенным/снятым**.

### 4.2 Силовой ключ + вибромотор (low-side)

```
            VBAT (+ батареи, 3.0–4.2V)
              │
              ├───────────────────┐
              │                    │
            ┌─┴─┐                  │
            │ M │ вибромотор      ─┴─  D_flyback
            │   │ 1030 3V          ▼   1N4148 / SS14
            └─┬─┘                 ─┬─  (катод к VBAT+,
              │                    │    анод к стоку)
              ├────────────────────┘
              │
          Drain (сток)
              │
   GPIO   ┌───┴───┐
    │     │   N   │  MOSFET N-канал
    └─[Rg]┤ Gate  │  AO3400 (или BMS-ключ, если прошёл тест Vds)
    │     │       │
  [Rpd]   └───┬───┘
    │      Source (исток)
    │         │
   GND ───────┴──── GND
```

Номиналы и назначение:
- **Rg (в гейт, последовательно): 100–470 Ом** — ограничивает бросок заряда
  затвора и звон.
- **Rpd (гейт→исток, pulldown): 10–100 кОм** — КРИТИЧНО. Держит транзистор
  закрытым, пока GPIO в Hi-Z (deep sleep, загрузка), иначе мотор жужжит сам.
- **D_flyback параллельно мотору**, катод к VBAT+, анод к стоку — гасит
  индуктивный выброс. Schottky (SS14) чуть лучше 1N4148.
- **MOSFET**: AO3400 (logic-level, SOT-23) или BMS-ключ — только после теста
  Vds < 50–100 мВ при 3.3V на гейте.
- Мотор: top → VBAT+, bottom → сток. Узел «мотор–сток» общий с анодом диода.

Логика: GPIO=HIGH → MOSFET открыт → ток VBAT → мотор → GND, вибрация.
GPIO=LOW/Hi-Z → Rpd закрывает MOSFET → мотор выключен.

---

## 5. Резерв: измерение заряда батареи (на будущее)

В текущей сборке НЕ разведено и в прошивке отключено. Сохранено, чтобы можно
было вернуться. Батарея — Li-ion до 4.2 В.

### Зачем делитель
ADC ESP32-C3 меряет ~0–2.5 В (аттенюация 11 dB; вход нельзя выше ~3.1 В).
Батарею 4.2 В надо поделить. Делитель из двух равных резисторов делит на 2:
`V_adc = VBAT · R2/(R1+R2)`, при R1=R2 → 4.2 В → 2.1 В, 3.0 В → 1.5 В (в
диапазоне ADC). В прошивке обратный множитель `VDIV_RATIO = (R1+R2)/R2 = 2.0`.

### Схема узла (отвод от сырой шины VBAT, НЕ от 3.3 В)
```
   VBAT (+ батареи, 3.0–4.2 В)
     │
    ┌┴┐
    │ │ R1   (верхнее плечо)
    └┬┘
     ├───────────────┬──────────►  на ADC-пин (GPIO0..3 = ADC1; GPIO4 занят мотором)
    ┌┴┐               │
    │ │ R2          [C 100нФ]   (буфер для стабильного замера)
    └┬┘               │
     │                │
    GND ──────────────┴
```

### Номиналы (компромисс ток ↔ точность; делитель течёт 24/7!)
| R1 = R2 | Постоянный ток | Комментарий |
|---|---|---|
| 100 кОм | ~21 мкА | проще для ADC, но «грызёт» батарею |
| 220 кОм | ~10 мкА | разумный баланс (нужен C 100нФ) |
| 1 МОм | ~2 мкА | минимум тока, обязательны C + усреднение |

Лучший для носимого вариант — **гейтировать делитель транзистором** (включать
GPIO только на момент замера), тогда в сне ток = 0. Цена — +1 транзистор.

### Что вернуть в прошивке при добавлении
- ADC-импорты, функция `read_battery_mv()`, константа `VDIV_RATIO`.
- Характеристика BLE `…0002` (u16 LE, мВ) — UUID уже зарезервирован.
- Завести среднюю точку на свободный ADC1-пин (GPIO0..3).

---

## 6. Зафиксированная разводка (факт)
- Питание платы: выход LDO 3.3 В → контакты `3.3` и `G`.
- Вибромотор (затвор MOSFET): **GPIO3** (нужен внешний pulldown Rpd).
- Кнопка настройки: **отдельная тактовая кнопка на GPIO4** (в диапазоне
  GPIO0..5, будит C3 из deep sleep). Кнопка на GND + внешний pull-up **80 кОм**
  на 3V3 (ток при нажатии ≈41 мкА). Долгое нажатие (≥2 с) → режим настройки.
- Время суток переживает deep sleep, при входе в настройку НЕ сбрасывается
  (re-sync нужен только после полной потери питания).
- Встроенные BOOT/RST не используются.
- Замер батареи: не разведён (см. §5).

---

## 7. Открытые вопросы / TODO перед сборкой
- [ ] Измерить ток покоя всей силовой цепи в deep sleep (цель: < 1–2 мА, лучше мкА).
- [ ] Тест MOSFET с BMS на полное открытие от 3.3V (замер Vds). При провале — AO3400.
- [ ] Подтвердить, что плата ESP32-C3 не мешает выбранной схеме питания.
- [ ] Уточнить: пользоваться во время зарядки? (определяет нужность load-sharing)
- [ ] Нужна ли обратная связь плата→приложение (статус, «след. сигнал через N мин»)?
